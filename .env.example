# GAMS Environment Configuration
# คัดลอกไฟล์นี้เป็น .env แล้วแก้ไขค่าก่อน deploy
# cp .env.example .env

# ============================================================
# DATABASE
# ============================================================
# รหัสผ่าน MySQL — ต้องเปลี่ยนก่อน deploy (ห้ามใช้ค่า default บน production)
MYSQL_ROOT_PASSWORD=change_this_strong_password

# Port ที่ expose MySQL ออกนอก container (ปิดได้ถ้าไม่ต้องการ access โดยตรง)
MYSQL_PORT=3306

# ============================================================
# BACKEND
# ============================================================
# JWT Secret — ใช้ random string ยาวอย่างน้อย 64 ตัวอักษร
# สร้างได้ด้วย: openssl rand -hex 32
JWT_SECRET=change_this_to_a_very_long_random_secret_string_at_least_64_chars

# Port ที่ expose Backend API
BACKEND_PORT=3000

# ============================================================
# RADIUS (FreeRADIUS)
# ============================================================
# RADIUS Secret — ต้องตรงกับ RADIUS server ของมหาวิทยาลัย
RADIUS_SECRET=testing123

# ============================================================
# FRONTEND
# ============================================================
# Port ที่ expose Frontend
# สำหรับ production บน server: ใช้ FRONTEND_PORT=80
FRONTEND_PORT=4300

# ============================================================
# phpMyAdmin
# ============================================================
# Port สำหรับเข้า phpMyAdmin
# สำหรับ production: ปิด service นี้ใน docker-compose.yml หรือ block ด้วย firewall
PMA_PORT=8888
