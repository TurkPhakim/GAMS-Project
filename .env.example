# GAMS Environment Configuration
# คัดลอกไฟล์นี้เป็น .env แล้วแก้ไขค่าก่อน deploy
# cp .env.example .env

# ============================================================
# SERVER
# ============================================================
# Domain ของ server (ใช้สำหรับ nginx และ Let's Encrypt)
DOMAIN=gams.youruniversity.ac.th

# Email สำหรับรับแจ้งเตือนจาก Let's Encrypt (cert หมดอายุ ฯลฯ)
CERTBOT_EMAIL=admin@youruniversity.ac.th

# IP ที่อนุญาตให้เข้า phpMyAdmin — ใส่ IP ของทีม admin
# หลาย IP: ใช้ nginx syntax เช่น ขึ้นบรรทัดใหม่แต่ละ IP
# ตัวอย่าง IP เดียว:  ADMIN_IP_RANGE=161.200.xxx.xxx
# ตัวอย่าง subnet:    ADMIN_IP_RANGE=161.200.0.0/16
ADMIN_IP_RANGE=127.0.0.1

# Path สำหรับเข้า phpMyAdmin — เปลี่ยนให้คาดเดายาก (ห้ามใช้ phpmyadmin หรือ pma)
PMA_SECRET_PATH=db-manage-app

# ============================================================
# DATABASE — MySQL root (ใช้สำหรับ init และ phpMyAdmin เท่านั้น)
# ============================================================
# รหัสผ่าน MySQL root — ต้องเปลี่ยนก่อน deploy
MYSQL_ROOT_PASSWORD=change_this_strong_root_password

# Port ที่ expose MySQL ออกนอก container (ปิดได้บน production)
MYSQL_PORT=3306

# Performance tuning — ปรับตาม RAM ของ server
# innodb_buffer_pool_size แนะนำ 50-70% ของ RAM
# server 2GB RAM → 1G, server 4GB RAM → 2G, server 8GB RAM → 4G
MYSQL_INNODB_BUFFER_POOL_SIZE=512M
MYSQL_MAX_CONNECTIONS=150

# ============================================================
# DATABASE — App user (ใช้โดย backend — สิทธิ์จำกัด)
# ============================================================
MYSQL_APP_USER=gams_app
MYSQL_APP_PASSWORD=change_this_app_password

# ============================================================
# BACKEND
# ============================================================
# JWT Secret — ใช้ random string ยาวอย่างน้อย 64 ตัวอักษร
# สร้างได้ด้วย: openssl rand -hex 32
JWT_SECRET=change_this_to_a_very_long_random_secret_string_at_least_64_chars

# ============================================================
# RADIUS (FreeRADIUS)
# ============================================================
RADIUS_SECRET=testing123
